global:
  resolve_timeout: 5m

route:
  receiver: 'default'
  group_by: ['alertname', 'service']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 3h

receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://localhost:9999/dev-null'
        send_resolved: true
