#!/usr/bin/env bash
# Generated by infra/scripts/generate-kafka-artifacts.mjs. Do not edit.
# Canonical Kafka topic creation
set -euo pipefail

BROKER="${KAFKA_BOOTSTRAP_SERVERS:-kafka:9092}"
ADMIN_CONFIG="${KAFKA_ADMIN_CONFIG:-/tmp/admin.properties}"
test -f "$ADMIN_CONFIG" || { echo "Kafka admin config is required: $ADMIN_CONFIG" >&2; exit 65; }

kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic messaging.message.sent --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic product-events --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic video-events --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.created --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.updated --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.paid --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.shipped --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.cancelled --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.confirmed --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.delivered --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.completed --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.refund.requested --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.refunded --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.chargeback.created --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.chargeback.resolved --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.webhooks.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.webhooks.dlt --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic inventory.release-requested --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic inventory.released --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic inventory.reservation-expired --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic shipping.cancel-requested --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic shipping.cancelled --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic shipping.status.updated --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic shipping.cod.collected --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic notification.events --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic notification.seller-invoice-required --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic notification.alert --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic product.approved --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic product.rejected --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic review.replied --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic return.requested --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payout.completed --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic user.registered --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic user.password-reset --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic video.upload.completed --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic video.transcode.completed --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic video.transcode.failed --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic video.moderation.completed --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic video.published --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic video.rejected --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic video.moderation.dlt --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic gdpr.export-requested --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic gdpr.export-fragment --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic gdpr.deletion-requested --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic gdpr.deletion-completed --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic settlement.hold --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic seller.finance.adjustment --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.created.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.created.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.updated.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.updated.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.paid.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.paid.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.shipped.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.shipped.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.cancelled.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.cancelled.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.confirmed.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.confirmed.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.delivered.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic order.delivered.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.completed.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.completed.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.refund.requested.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.refund.requested.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.refunded.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.refunded.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.chargeback.created.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.chargeback.created.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.chargeback.resolved.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic payment.chargeback.resolved.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic inventory.release-requested.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic inventory.release-requested.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic inventory.released.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic inventory.released.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic shipping.cancelled.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic shipping.cancelled.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic shipping.cod.collected.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic shipping.cod.collected.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic settlement.hold.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic settlement.hold.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic seller.finance.adjustment.retry --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic seller.finance.adjustment.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic video.upload.completed.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
kafka-topics --bootstrap-server "$BROKER" --command-config "$ADMIN_CONFIG" --create --if-not-exists --topic video.transcode.completed.DLT --partitions 6 --replication-factor 3 --config min.insync.replicas=2
