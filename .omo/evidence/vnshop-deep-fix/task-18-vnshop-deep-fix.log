Task 18 observability verification
Date: 2026-08-25
Worktree: C:\Users\dangq\AppData\Local\Temp\opencode\vnshop-deep-fix-pr

Red/static baseline:
- rg required metric names and alert names before implementation: no matches.
- Existing RED dashboard only covered request rate, 5xx ratio, and p50/p95/p99 by job; no Saga/Outbox/DLT/Cache rows.
- infra/scripts/backup-cron.sh had no overlap lock.

Implementation:
- Added repository-backed Micrometer gauges for outbox oldest age, dead outbox total, saga compensation age, stuck compensation count, and DLT age in order-service.
- Added payment_orphan_total and provider_latency_seconds instrumentation in payment-service, plus payment DLT age gauge.
- Added redis_evictions_total, cache hit/miss counters, and Redis operation timing in cart-service.
- Added OutboxStale, SagaCompensationStuck, DltStale, RedisEvictions, and ProviderLatencyHigh rules with bounded labels.
- Added Grafana saga-outbox-cache dashboard with RED per route, Saga/Outbox/DLT, Cache/provider rows.
- Added atomic backup-cron lock directory with exit code 75 and backup-overlap fixture.

Commands/results:
- services/order-service/./mvnw.cmd -q -DskipTests compile: PASS (exit 0).
- services/payment-service/./mvnw.cmd -q -DskipTests compile: PASS (exit 0).
- services/cart-service/npm test -- --runInBand: PASS (10 suites, 60 tests).
- python -m pytest infra/scripts/test_backup_overlap_contract.py infra/scripts/test_observability_contract.py: PASS (4 tests).
- python JSON parse of infra/grafana/dashboards/saga-outbox-cache.json: PASS.
- git diff --check: PASS; pre-existing CRLF warnings only.
- promtool test rules infra/prometheus/slo-rules.test.yml: BLOCKED, promtool unavailable.
- docker compose ps prometheus: BLOCKED, compose interpolation requires missing POSTGRES_PASSWORD.
- Live Prometheus curl query: BLOCKED, no usable Prometheus container.
- LSP diagnostics: BLOCKED, OMC LSP daemon timed out.
- ast-grep: BLOCKED, @ast-grep/napi unavailable per prior task notes.

Manual QA:
- Static dashboard JSON loaded successfully and required row titles were asserted.
- Backup overlap fixture asserted atomic lock path and Kubernetes Forbid policy.
- Cart service tests exercised the real NestJS metrics module path without changing product behavior.

Cleanup receipt:
- No commits, pushes, resets, or destructive cleanup performed.
- Only task-18 observability files and append-only evidence/notepad entries were changed by this task.

Risks/blockers:
- Live scrape output, Prometheus rule evaluation, and alert delivery remain Docker-gated.
- Repository LSP was unavailable during this verification window.
