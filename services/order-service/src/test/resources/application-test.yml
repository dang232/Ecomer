spring:
  jpa:
    hibernate:
      ddl-auto: validate
  kafka:
    consumer:
      auto-offset-reset: earliest

vnshop:
  admin-cursor:
    secret: test-secret
    ttl: PT1H
  datasource:
    read-replica:
      enabled: false
